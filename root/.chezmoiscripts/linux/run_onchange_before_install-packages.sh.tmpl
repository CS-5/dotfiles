{{ if eq .osid "linux-debian" "linux-ubuntu" -}}

{{ $packages := list
  "fish"
  "fzf"
  "asciinema"
  "fd-find"
  "lsd"
  "httpie"
  "curl"
  "git"
  "wget"
  "unzip"
  "jq"
-}}

{{ $sudo := "sudo" -}}
{{ if eq .chezmoi.username "root" -}}
{{   $sudo = "" -}}
{{ end -}}

#!{{ lookPath "bash" }}

set -eufo pipefail

{{ $sudo }} apt-get update
{{ $sudo }} apt-get install -y {{ $packages | join " " }}

{{ end -}}